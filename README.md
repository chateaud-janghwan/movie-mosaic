# Movie Mosaic

Browser-based face mosaic video editor.

## Cloudflare Pages

Use these settings:

- Framework preset: None
- Build command: leave empty
- Build output directory: `/`
- Root directory: repository root

The app is a static site made of `index.html`, `styles.css`, and `app.js`.
